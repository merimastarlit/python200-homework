{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Current File (arm64 venv)",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "python": "${workspaceFolder}/.vscode/python-arm64"
        }
    ]
}
